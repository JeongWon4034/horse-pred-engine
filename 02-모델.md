# 02 · 모델 설계

## 먼저 — 이미 나와 있는 것들

| 구분 | 실체 | 쓰는 기술 |
|---|---|---|
| 해외 정석 | **Bill Benter** — 1980년대부터 홍콩 경마 | 조건부 로지스틱 회귀. **40년째 이게 표준** |
| 국내 상용 | 스마트AI경마(2019 특허), 경마선생, 신마신, 코리아레이스 마패 | 비공개. 예측값만 제공 |
| 취미·학교 | GitHub 다수, 홍콩 데이터셋이 흔함 | RandomForest, 로지스틱, LGBM |

**결론 둘:**
1. 모델 자체로는 차별화 못 한다. 다들 비슷한 걸 쓴다.
2. 위 전부가 "정답 하나 알려주는" 서비스다. **유저가 자기 성향의 모델을 만드는 건 아무도 안 한다.**

참고
- Benter 논문 해설: https://actamachina.com/posts/annotated-benter-paper
- 백테스트 포함 예제: https://github.com/ethan-eplee/HorseRacePrediction

---

## 랭킹은 모델 이름이 아니라 학습 방식

어떤 구조를 쓰든 **손실함수만 바꾸면** 랭킹 모델이 된다.
"무슨 모델을 쓰냐"와 "랭킹이냐"는 별개의 선택.

| 방식 | 학습에 쓰는 것 | 채택 |
|---|---|---|
| Pointwise | 말 한 마리씩 따로 점수. 일반 분류·회귀 | ✕ |
| Pairwise | 두 마리씩 비교. RankNet, LambdaRank | ✕ |
| **Listwise** | 경주 전체를 놓고 순위 확률을 직접 최적화 | **○** |

### 핵심은 이 세 줄이 전부

```python
# 경주 하나 = 출전마 10마리를 한 번에 입력
scores = model(features)          # [10]  말마다 점수 하나
probs  = softmax(scores)          # [10]  합이 자동으로 1.0
loss   = -log(probs[실제_1등])     # 1등 확률을 최대화
```

`softmax`가 **경주 안에서** 걸리는 게 전부다. 이것 하나 때문에:
- 확률 합이 100%로 자동으로 맞음
- 같은 경주 말들끼리 경쟁 관계가 생김 (한 마리 오르면 나머지 내려감)

일반 분류는 말마다 따로 sigmoid를 걸어 합이 안 맞고, 그러면 배당 계산이 불가능하다.

> 이 형태가 이미 **조건부 로지스틱 회귀 = Plackett–Luce = ListNet**의 공통 뼈대.
> 이름만 다르고 같은 것. 제일 단순한 버전이 Benter가 40년 전에 쓴 그거다.

---

## 1~10등을 다 맞추는 게 아니다

모델 출력은 항상 **확률 10개**일 뿐, 순열을 뱉지 않는다.
바뀌는 건 학습할 때 정답을 얼마나 쓰느냐 하나.

| 범위 | 내용 | 판단 |
|---|---|---|
| 1등만 | "10마리 중 3번이 이겼다" | 1단계. 제일 단순 |
| **top-3** | "3번, 그 다음 7번, 그 다음 1번" | **목표.** for문 하나 추가 |
| 전체 순위 | 10등까지 전부 | **하지 않음** |

전체 순위를 쓰면 오히려 나빠진다. 7~10등은 이미 승부가 난 뒤라 순서가 거의 랜덤이고,
억지로 맞히려 하면 노이즈를 학습한다.
마침 우리가 다루는 게 단승·복승·삼복승이라 **1~3착이 딱 필요한 범위**.

```python
# top-3 — "1등 뽑고 → 걔 빼고 → 2등 뽑고 → 걔 빼고 → 3등 뽑고"
loss, 남은말 = 0, 전체
for 말 in [1등말, 2등말, 3등말]:
    loss -= log_softmax(scores[남은말])[말]
    남은말.remove(말)
```

> **직관과 반대인 부분**
> 데이터가 1만 경주뿐이라 **정답을 더 쓸수록 학습이 쉬워진다.**
> 1등만 쓰면 배울 거리 1만 개, top-3까지 쓰면 실질 3만 개.
> **데이터가 적을수록 listwise가 유리하다.**

---

## 모델 구조는 단계적으로 키운다

**손실함수는 1~5단계 내내 안 바뀐다.** `model()` 자리만 갈아끼우면 되고,
단계마다 비교 숫자가 나와서 발표 자료가 저절로 채워진다.

```python
# 1단계 — 조건부 로지스틱 회귀 (베이스라인)
scores = features @ W

# 2단계 — 신경망 랭커
scores = MLP(features)

# 3단계 — 말의 경력을 시퀀스로  ★ 딥러닝의 본체
h = TransformerEncoder(과거_출전기록_시퀀스)
scores = MLP(concat(h, features))

# 4단계 — 경주 안에서 말끼리 참조  ★ 회귀가 절대 못 하는 것
h = SelfAttention(경주_내_10마리_전체)
scores = MLP(h)

# 5단계 — 6축 분리 + 유저 가중치
s_k    = [Tower_k(h) for k in 6개축]
scores = sum(w[k] * s_k[k])
```

**1단계는 버리지 않는다 — 비교 기준선.**
없으면 "이게 좋은 건지 어떻게 아세요?"에 답을 못 한다. 반나절 투자로 발표 슬라이드 한 장.

---

## 딥러닝을 쓰는 정당한 이유 세 개

조건부 로지스틱 회귀가 **구조적으로 못 하는 것**. 억지로 얹는 게 아니다.

### ① 말의 경력은 시퀀스다

```
지금 계획:  과거 12번 출전 → 사람이 요약 → "최근 5경주 평균 4.2"
```

12경기가 숫자 하나로 뭉개진다. "5경기"도 우리가 임의로 정한 것.
시퀀스 인코더는 **자를 지점을 정할 필요가 없고**, 상승세·회복 패턴·컨디션 주기를 스스로 잡는다.
→ **이 프로젝트에서 제일 정당한 딥러닝 사용처. 볼륨도 제일 크다.**

### ② 경주는 말들의 집합이다

기획서의 그 문장:
> "선행마가 4두 몰려 있으면 초반 오버페이스라 추입마가 유리해지는 효과"

회귀는 한 행(말 한 마리)만 보므로 **절대 못 배운다.**
하려면 `선행마_수` 피처를 사람이 손으로 만들어야 하고, 그게 맞는지는 영원히 모른다.
Attention이면 자동으로 학습된다.

### ③ 6축을 따로 학습하면 척도가 안 맞는다

따로 만들면 축마다 점수 단위가 달라(어떤 건 확률, 어떤 건 로그오즈, 어떤 건 초)
`w₁·s₁ + w₂·s₂`가 무의미해진다. 슬라이더 "기수 40"이 실제로 40의 영향력이 아니게 된다.

**한 네트워크 안에서 6갈래로 함께 학습**해야 척도가 자동으로 맞는다.

> 기획서에 "성능 일부를 포기하고 조합 가능성을 강조한 것"이라고 적어둔 **트레이드오프를
> 포기하지 않아도 된다.** 함께 학습하니 축 간 상호작용도 배우고, 축 간 예측 상관 0.9 이하
> 기준도 통과하기 쉬워진다.

---

## 유저 가중치 — 재학습 없이 되는 이유

축별 점수 `s₁…s₆`를 미리 계산해 캐시해두면,
유저 조작은 결합 계수만 바꾸는 **곱셈·덧셈**이라 1초 미만에 끝난다.

```
말 한 마리당:
  최종 = w최근·s최근 + w부모·s부모 + w기수·s기수
       + w거리날씨·s거리날씨 + w주행·s주행 + w인기도·s인기도
         ↑ 유저가 정함(합 100)    ↑ 축 모델 점수, 미리 계산·캐시
```

### 이 프로젝트에서 제일 내세울 장치 — 랜덤 가중치 학습

그냥 만들면 학습 때 안 본 가중치 조합에서 무너진다.
그래서 **학습 배치마다 `w`를 디리클레 분포에서 새로 뽑아** 쓴다.

```python
매 스텝:  w ~ Dirichlet(α)        # 예: [0.05, 0.6, 0.1, 0.05, 0.15, 0.05]
          최종 = Σ wₖ·sₖ
          역전파
```

효과 셋:
1. 가끔 한 축에 몰리니 **각 타워가 혼자서도 쓸모 있어야 함** (프리셋 "이변형"이 실제로 동작)
2. 어떤 조합이 와도 안 무너짐 → 유저 커스텀이 안전
3. 드롭아웃 같은 정규화 효과 → 데이터 적은 상황에 유리

> 표준 기법 이름이 붙은 건 아니고 우리가 조립한 설계다 (조건부 네트워크·MoE 계열 아이디어).
> **"유저가 모델을 실시간으로 바꾸는데 왜 재학습이 필요 없나요?"에 대한 정확한 답**이기도 하다.

---

## 스코어 계약 (반드시 먼저 고정)

6축을 나눠 작업하기 전에 이걸 박아야 한다. 나중에는 못 고친다.

> **모든 축 모델은 `(race_id, horse_no) → 경주 내에서 표준화된 z-score`를 반환한다.**
> **경주별 평균 0, 표준편차 1.**

이게 없으면 가중합이 무의미해지고, 슬라이더가 동작하지 않거나 한 축이 다 먹는다.

---

## 확률 · 시뮬레이션

Plackett–Luce가 이미 확률 모델이라 앞뒤가 자연스럽게 맞는다.

```
타워 스코어 → PL 분포 → 순열 1만 개 샘플링
                          ├─ 1착 · top3 · 삼복승 조합 확률
                          ├─ 중간 베팅 시점 조건부 확률
                          └─ 샘플 하나를 3D 리플레이로 렌더링
```

**"시뮬레이션"이 세 가지 다른 뜻으로 쓰이고 있으니 구분할 것:**

| | 실제로 하는 일 | AI인가 | 필요한가 |
|---|---|---|---|
| ① 몬테카를로 | 점수 → 배팅용 확률로 변환 | ✕ 통계 계산 | **조건부** |
| ② 3D 레이스 화면 | 말이 뛰는 걸 그림 | ✕ 그래픽 | 경기 후 재생만 |
| ③ 예측 모델 | 어느 말이 빠른지 학습 | **○ 이게 AI** | 필수 |

**①의 필요 여부:**
- 단승(1착)만 → **불필요.** softmax가 이미 1착 확률
- 복승·삼복승 조합 → 필요 (조합 확률은 닫힌 식이 지저분)
- 중간 베팅 → 필요 (조건부 확률)

**주의: 독립 샘플링 금지.** 말마다 따로 뽑으면 "빠른 페이스 → 추입마 전원 유리"의 상관을 못 잡는다.
경주 단위 공통 충격을 하나 넣으면 해결:

```
pace_shock ~ N(0, σ)              # 경주당 1회 추첨
기록ᵢ = μᵢ + βᵢ·pace_shock + εᵢ    # βᵢ = 각질(선행/추입)
```

단, 4단계 Attention이 이미 그 상호작용을 배우므로 **필요하면 나중에 넣는 옵션.**

---

## 구현 — 직접 짠다

기성품이 있긴 하다: **allRank**(PyTorch), **TensorFlow Ranking**, LightGBM `lambdarank`, XGBoost `rank:*`

**그래도 직접 짠다:**
1. 손실함수가 3~5줄이라 라이브러리 쓰는 게 더 번거롭다
2. 5단계(6축 + 랜덤 가중치)는 기성품에 없어 **어차피 커스텀**
3. 포트폴리오에서 "라이브러리 호출"과 "손실함수 직접 구현"은 완전히 다르게 읽힌다

---

## 설명은 공짜로 나온다

기획서 미해결 항목 — "피처에서 뭘 조절해야 할지 감이 안 옴"의 답.
축 구조 덕분에 SHAP 없이 기여도가 그냥 계산된다.

```
기여도ₖ = wₖ · sₖ  →  정규화

"이 예측의 62%는 기수 축이 만들었습니다.
 기수를 낮추면 3번마가 5번마로 바뀝니다."
```

단일 대형 모델이면 SHAP 붙이고 해석하느라 고생할 지점이 축 분리 덕에 자동 해결.
**발표에서 강조할 곳.**

---

## 검증

- **반드시 시간순 분할.** 랜덤 분할하면 미래로 과거를 맞히는 셈이라 숫자가 전부 거짓말
- 대표 지표는 **ROI** ([03-데이터.md](03-데이터.md) 참조)
- **캘리브레이션**: 41%라 한 것들이 실제로 41% 맞았나 — "모델 믿을 만하다"의 유일한 증거
- 축 간 예측 상관 **0.9 이하**가 합격선
- **"순열을 정확히 맞혔나"는 안 잰다.** 10마리 순열이 360만 가지라 의미 없음
