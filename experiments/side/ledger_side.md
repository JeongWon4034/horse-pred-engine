# 사이드 실험 장부 (브랜치 `exp/side`)

본 실험(`feat/dl-stages`, `experiments/ledger.md`)은 건드리지 않고 옆에서 돌린 것들. 기존 코드 수정 없음 — 새 파일만
(`model/score.py` `model/side_train.py` `model/side_roi.py` `model/side_ensemble.py` `side_sweep.sh`).
같은 자(팀 채점기), 같은 데이터(f17ca36), 같은 규칙(game 학습 금지·test 미개봉·경주 단위 셔플·hrNo 미사용).

## 1. game ROI — 학습된 모델로 game 3,700경주 점수 → 실배당 단승 1단위

`model.score --split game` 이 체크포인트(`experiments/runs/*.pt`)와 train 에서 다시 fit 한 전처리로 game 점수를 뽑는다.
이력은 train∪valid 풀에서 `rcDate < 경주일` 만 (game 행은 풀에 없음). LightGBM 은 같은 train 으로 재학습해 game 예측.

### 1-1. 결과 (전략 top1 = 매 경주 모델 1위마)

| 모델 | 베팅 | 적중 | ROI | 적중 평균배당 |
|---|---:|---:|---:|---:|
| 시장 (인기 1위마) | 3,687 | 37.3% | −20.7% | 2.12 |
| LightGBM 73 | 3,678 | 33.0% | −5.8% | 2.86 |
| LightGBM 77 (배당 포함) | 3,687 | 38.2% | −13.4% | 2.27 |
| S3 L10 단일 | 3,679 | 34.4% | −1.0% | 2.88 |
| S3 L10 seed5 평균 | 3,680 | 34.6% | −0.7% | 2.87 |
| S3 L20 seed5 평균 | 3,679 | 34.6% | **+0.5%** | 2.90 |
| LGB73 + S3 L20 (5:5) | 3,680 | 34.1% | −3.8% | 2.82 |

조건부 베팅(모델 확률 − 시장 확률 ≥ t 인 경주만): S3 L20 t=0.10 에서 1,136베팅 **+37.2%**, LGB t=0.10 +18.4%.
EV(확률×배당) 최대 말 베팅: S3 +19~26%, LGB −13%.

### 1-2. 이게 진짜인가 — 누수 점검

- **피처 누수 없음.** 첫 출전 말(F1_first_start=1)의 생애 성적이 game·valid·train 전부 NaN(현재 경주 결과 미포함). F1/F2/F3/F4 피처와 y_win 상관이 세 분할에서 동일(예: F1_win_rate_life +0.163/+0.166/+0.168).
- **적중률은 valid 와 같다.** LGB game 33.0% vs valid 34.0%, S3 34.6% vs 34.8%. 모델이 game 에서 더 잘 맞히는 게 아니다.
- **그런데 valid 에서 같은 전략을 재현하면 수익이 사라진다** (valid 에 배당 컬럼이 없어 `0.800/F6_mkt_prob` 로 역산 — game 에서 검증: 이 추정으로 시장 ROI −20.7% 정확히 재현):

| valid 1,254경주 | 적중 | ROI | 적중배당 |
|---|---:|---:|---:|
| 시장 | 39.3% | −16.6% | 2.12 |
| LightGBM 73 | 34.0% | −9.3% | 2.67 |
| S3 L20 seed5 평균 | 34.8% | −8.6% | 2.63 |
| LGB73 + S3 (5:5) | 35.3% | −7.7% | 2.61 |
| 조건부 t=0.03~0.10 (베팅 240~600) | 24~29% | −0.2 ~ +13.0% | 3.6~4.6 |

- **해석**: game 의 좋은 ROI 는 **시기 혼재** 때문이다. game 은 2015~24 개최일의 15% 를 뺀 것이고 train 은 그 사이사이 날짜를 전부 포함한다. 모델은 "그 시즌에 누가 잘 뛰었는지"를 이미 봤다(적중률은 같지만 배당 높은 말 쪽으로 정확히 기운다). valid(2025-11~2026-05, train 뒤)는 이 이점이 없다.
- **정직한 기대치는 valid 값이다**: 모델은 인기마보다 **8%p 덜 잃는다**. 매 경주 베팅으로 이익은 안 난다. 조건부 베팅은 손익분기 근처지만 베팅 수가 적어(±15%p) 확정 못 한다.
- **게임 서비스 관점**: 유저가 플레이하는 game 경주에서 AI 가 실제보다 강하게 보인다는 뜻. 온보딩 프리셋 ROI 표기는 game 값이 아니라 valid 값(또는 그 사이의 보수적 값)을 써야 한다. 이건 팀에 알릴 사항.

## 2. S3 하이퍼파라미터 탐색 (`side_sweep.sh`, 73피처, L=20, 구성마다 seed 2개)

18개 구성 × 2 seed = 36회. 한 회 22~67초(배치 크기와 조기 중단 시점에 따라). 로그 `experiments/runs/logs\side_20260902_184020/`.

| 구성 | top-1 (seed 20260901 / 1) | 평균 (vs 기본) | logloss 평균 (vs 기본) | ECE | 최고 epoch |
|---|---|---:|---:|---:|---|
| 기본: GRU64 · dropout 0.2 · 임베딩 16 · 헤드 128 · lr 1e-3 · wd 1e-2 · 배치 256 · L20 | 35.17 / 34.53 | 34.85 (+0.00) | 1.8569 (+0.0000) | 0.0051 | 13 / 10 |
| GRU 32 | 34.61 / 33.89 | 34.25 (-0.60) | 1.8652 (+0.0083) | 0.0049 | 8 / 5 |
| GRU 128 | 34.53 / 33.89 | 34.21 (-0.64) | 1.8668 (+0.0099) | 0.0054 | 6 / 4 |
| dropout 0.1 | 34.37 / 34.21 | 34.29 (-0.56) | 1.8676 (+0.0107) | 0.0078 | 3 / 9 |
| dropout 0.35 | 34.85 / 33.97 | 34.41 (-0.44) | 1.8530 (-0.0039) | 0.0056 | 10 / 8 |
| 임베딩 8d | 34.21 / 34.69 | 34.45 (-0.40) | 1.8589 (+0.0020) | 0.0057 | 5 / 7 |
| 임베딩 32d | 34.29 / 33.65 | 33.97 (-0.88) | 1.8639 (+0.0071) | 0.0080 | 5 / 4 |
| 헤드 256 | 34.13 / 34.45 | 34.29 (-0.56) | 1.8668 (+0.0099) | 0.0051 | 6 / 3 |
| lr 5e-4 | 35.41 / 34.61 | 35.01 (+0.16) | 1.8559 (-0.0010) | 0.0046 | 13 / 12 |
| lr 2e-3 | 34.45 / 34.29 | 34.37 (-0.48) | 1.8670 (+0.0102) | 0.0050 | 4 / 10 |
| wd 1e-3 | 35.17 / 34.37 | 34.77 (-0.08) | 1.8569 (+0.0001) | 0.0051 | 13 / 10 |
| wd 5e-2 | 35.09 / 34.13 | 34.61 (-0.24) | 1.8687 (+0.0118) | 0.0078 | 8 / 4 |
| 배치 128 | 34.61 / 34.45 | 34.53 (-0.32) | 1.8669 (+0.0100) | 0.0089 | 8 / 11 |
| 배치 512 | 34.29 / 33.97 | 34.13 (-0.72) | 1.8716 (+0.0147) | 0.0065 | 8 / 4 |
| 손실 PL topk=3 | 34.45 / 34.29 | 34.37 (-0.48) | 1.8841 (+0.0273) | 0.0157 | 8 / 6 |
| <rare> 기준 20회 | 34.61 / 35.65 | 35.13 (+0.28) | 1.8603 (+0.0035) | 0.0057 | 8 / 12 |
| 이력 L=5 | 35.25 / 33.81 | 34.53 (-0.32) | 1.8615 (+0.0046) | 0.0060 | 8 / 12 |
| GRU128 + 헤드256 + dropout 0.3 | 34.53 / 34.13 | 34.33 (-0.52) | 1.8594 (+0.0025) | 0.0083 | 7 / 6 |

읽기:
- **기본 구성을 seed 잡음(±0.5%p)을 넘어 이기는 구성이 없다.** 평균 top-1 최고는 `<rare>` 20회(35.13, +0.28)와 lr 5e-4(35.01, +0.16)인데 둘 다 잡음 안이고, 두 seed 의 순서가 바뀐다(rare20 은 seed 1 이 35.65 로 최고 단일값, seed 0 은 34.61).
- **logloss 로 봐도 기본(1.8569)이 거의 최저.** 나은 건 dropout 0.35(1.8530, −0.004)뿐이고 top-1 은 −0.44. 
- **확실히 나쁜 것**: 배치 512(−0.72, logloss +0.015), 임베딩 32d(−0.88), PL topk=3(logloss +0.027, ECE 3배 — 1착 확률이 덜 날카로워짐), GRU 를 키우든 줄이든 −0.6.
- 결론: **S3 기본 설정이 안정점.** 본 실험의 S3 숫자(35.0 / seed5 34.6±0.5)는 튜닝 부족 때문이 아니다. 더 얻으려면 구조가 아니라 **데이터(경주 수)** 쪽이다.

## 3. 앙상블 가중치 · seed 개수 (`model.side_ensemble`, valid)

### seed 개수 곡선 — S3 L20 확률 평균 (부분집합 전체 평균)

| seeds | top-1 | logloss |
|---:|---:|---:|
| 1 | 34.63 | 1.8605 |
| 2 | 34.39 | 1.8516 |
| 3 | 34.48 | 1.8487 |
| 4 | 34.63 | 1.8472 |
| 5 | 34.77 | 1.8464 |
| 10 (L20 5 + L10 5) | 34.45 | 1.8493 |

logloss 는 seed 를 더할수록 단조 개선(1.8605 → 1.8464). top-1 은 34.4~34.8 사이에서 안 움직인다 — **seed 평균은 확률 품질을 올리고 적중률은 못 올린다.** L10 을 섞으면 오히려 조금 나빠짐(L20 5개가 최선).

### 가중치 w·S3 + (1−w)·LGB

| w | top-1 | logloss | vs LGB top-1 (CI) |
|---:|---:|---:|---|
| 0.0 (LGB) | 33.97 | 1.8846 | — |
| 0.3 | 34.93 | 1.8616 | +0.96 [−0.08, +1.99] |
| 0.4 | 35.17 | 1.8562 | +1.20 [+0.00, +2.39] |
| **0.5** | **35.33** | 1.8519 | +1.36 [−0.08, +2.79] |
| 0.7 | 35.01 | 1.8465 | +1.04 [−0.64, +2.71] |
| 1.0 (S3) | 34.77 | **1.8464** | +0.80 [−1.20, +2.79] |

위 표는 valid 전체로 w 를 본 것이라 **낙관값**이다. 교차 적합(경주를 반으로 갈라 한쪽에서 logloss 최소 w 선택, 다른 쪽 측정, 20회):
- 선택되는 w 평균 **0.86** (0.7~1.0) — logloss 기준으로는 S3 비중이 커야 한다
- 홀드아웃 top-1 **34.76** vs 같은 홀드아웃 LGB 33.97 → **+0.79%p**, logloss 1.8466
- 즉 "35.3" 은 w=0.5 를 valid 에 맞춘 값이고, 정직하게 고르면 **+0.8%p 근처**가 기대치. 본 장부의 판정("차이 없음, logloss 우위")과 일치.

권고: 서비스 확률은 **S3 L20 seed5 평균 0.8 + LGB 0.2** 근처. top-1 홍보 숫자는 35.3 이 아니라 34.8 을 쓴다.


## 4. S5 6축 타워 (`model.side_tower`, 기존 `models.TowerRanker` 그대로)

축마다 MLP(64) → 경주 내 z-score → 유저 가중치로 합산. 학습 중 가중치는 Dirichlet(α=0.7) 매 배치 샘플, 손실 PL topk=3. 평가는 균등 가중치.

| | 77피처 (F1~F6) | 73피처 (F1~F5) |
|---|---:|---:|
| 균등 가중 top-1 / top-3 | 36.6 / 66.4 (ep4) | 34.9 / 62.9 (ep18) |
| logloss | 1.8755 | 1.9251 |
| vs LGB top-1 | **−2.31 [−4.47, −0.24] — 유의하게 못함** | +0.96 [−0.72, +2.79] 차이 없음 |
| 축 단독 top-1 F1/F2/F3/F4/F5/F6 | 27.5 / 25.7 / 24.3 / 23.1 / 31.8 / 38.4 | 29.2 / 25.6 / 25.0 / 25.0 / 29.7 / — |
| 축 간 예측 상관 최대 | 0.62 (F1–F6) | 0.63 (F1–F5) |
| 혈통 100 | 25.7 | 25.6 |
| 기수 70 전적 30 | 29.0 | 30.0 |
| 인기도 0 나머지 균등 | 33.3 | (= 균등) 34.9 |

읽기:
- **축 간 상관 ≤ 0.9 목표는 여유 있게 통과**(최대 0.63). 슬라이더를 움직이면 예측이 실제로 달라진다.
- **축 단독 바닥 ≥ 25 는 77 에서 F3(24.3)·F4(23.1) 미달**, 73 에서는 전부 통과(25.0~29.7). 조건(F4) 축은 피처 7개뿐이라 단독으로 약한 게 자연스럽다. 무작위 10.2 보다는 훨씬 위.
- **77 균등 가중이 LGB 보다 유의하게 나쁘다(−2.3%p).** 랜덤 가중치로 학습하면 "어떤 가중치에서도 무너지지 않게" 되는 대신 특정 가중치에서의 최고점을 포기한다 — 스펙이 원하는 성질이지만 종합 정확도 비용이 2%p. F6 단독(38.4)이 시장(39.3) 근처인 건 인기도 축이 배당을 그대로 옮기기 때문.
- **73 균등 가중 34.9 는 LGB 와 동률**이고 S3(35.0) 과도 같다 — 인기도 없는 게임 모드가 필요하면 이 타워를 그대로 쓸 수 있다.
- 다음: 종합 헤드(가중치 고정 1)를 축 옆에 따로 두어 "균등 가중 정확도 손실" 을 없애는 변형, S3 이력 GRU 를 F1 축 입력으로 넣는 변형. game ROI 프리셋 3종은 §1 의 시기 혼재 문제 때문에 valid 역산 배당으로 재야 한다.


## 5. 온도 조절 (temperature scaling) — `model.side_temp`, valid

점수를 `T` 로 나눈 뒤 경주 내 softmax. 재학습 없음. 브랜치 `exp/temp-scaling`, 새 파일 `model/side_temp.py` 만 추가.

- 데이터 기준 커밋 **`05d8230`** — 팀 `common.py` 의 `load()` 가 `clean()` → `usable()` 로 바뀐 이후다(`39eeb26`, 2026-09-09 머지). **§1~4 의 `f17ca36` 값과 직접 빼서 읽으면 안 된다.**
- 이 커밋에서 재측정한 기준선: LightGBM 73 **33.49** / logloss 1.8899. S3 L20 seed 5개(20260901/1/2/3/4) top-1 34.4±0.3 (34.37/34.13/34.69/34.21/34.77), logloss 1.8642 평균
- `T` 그리드 0.60~2.00, 0.05 간격 29점. `T` 선택은 logloss 최소

### 5-1. 단조성 — 앙상블에서는 성립하지 않는다

단일 모델은 `T` 로 나누는 것이 단조 변환이라 **top-1 이 전 구간 고정**이다(코드가 assert 한다). 그런데 **seed 확률 평균은 다르다** — `T` 가 모델 간 상대 비중을 바꿔 argmax 가 옮겨간다. 실측으로 `T=0.60` 에서 35.01%, `T≥0.80` 에서 35.09% 로 움직였다. "온도는 순위를 안 바꾼다"는 단일 모델에만 쓸 수 있는 말이다.

### 5-2. valid 전체 최적 `T` (낙관값)

| 모델 | 현재 logloss (T=1) | 최적 T | 그때 logloss | ECE (T=1 → 최적) |
|---|---:|---:|---:|---|
| LightGBM 73 | 1.8899 | **0.85** | 1.8790 | 0.0131 → 0.0111 |
| S3 L20 단일 | 1.8775 | **1.05** | 1.8764 | 0.0091 → 0.0083 |
| S3 L20 seed5 평균 | 1.8490 | **0.95** | 1.8476 | 0.0068 → 0.0048 |

### 5-3. 교차 적합 (경주 반씩 갈라 한쪽에서 `T` 선택, 다른 쪽 측정, 20회 × 2분할)

| 모델 | 선택 T 평균 (범위) | 보정 전 | 보정 후 | 차이 |
|---|---:|---:|---:|---:|
| LightGBM 73 | 0.83 (0.75~0.90) | 1.8899 | **1.8809** | **−0.0091** |
| S3 L20 단일 | 1.06 (1.00~1.15) | 1.8775 | 1.8781 | +0.0006 |
| S3 L20 seed5 평균 | 0.94 (0.85~1.05) | 1.8490 | 1.8495 | +0.0005 |

### 5-4. 판정 — 가설 기각

**예상은 "신경망이 과신하므로 `T>1` 로 눌러 logloss 를 낮춘다" 였다. 반대였다.**

1. **S3 는 이미 보정되어 있다.** 최적 `T` 가 1.05 / 0.95 로 사실상 1이고, 교차 적합 이득이 +0.0006 — 즉 **없다**(오히려 미세 악화). ECE 도 이미 0.0091 / 0.0068 로 낮다. 온도로 짜낼 게 없다.
2. **미보정이었던 쪽은 LightGBM 이다.** 그런데 방향이 반대다 — 최적 `T`=0.83 으로 **확률을 더 뾰족하게** 해야 한다. lambdarank 점수를 softmax 로 확률화하는 과정에서 과소확신이 생긴 것으로 보인다.
3. **그래서 S3 의 logloss 우위가 좁아진다.** 양쪽을 같은 방식으로 보정한 홀드아웃 기준:

| | 보정 전 | 보정 후 | |
|---|---:|---:|---|
| S3 단일 − LGB | −0.0124 | **−0.0028** | 우위 거의 소멸 |
| S3 seed5 − LGB | −0.0410 | **−0.0314** | 우위 유지 |

**"확률 품질에서 일관되게 낫다"는 주장은 seed 5개 평균에서만 안전하다.** 단일 모델 우위는 LightGBM 을 보정하면 −0.0028 로 거의 사라진다. 발표에서 logloss 를 근거로 쓸 때는 **양쪽 다 보정한 값**을 쓰거나, seed 앙상블 기준임을 명시해야 한다.

### 5-5. 파생 — 3착 확률 과대평가는 온도 문제가 아니다

`docs/08` §5-7 의 "3착 확률이 강한 말을 9%p 과대평가(60% 이상 구간에서 모델 74% vs 실제 65%)" 는 온도로 설명되지 않는다. **1착 확률의 ECE 가 이미 0.0068~0.0091 로 잘 맞기 때문**이다. 즉 원인은 기저 확률이 아니라 **1착 확률에서 3착 확률을 유도하는 계산** 쪽이다. Plackett-Luce 를 쓰면 3착 확률은 몬테카를로나 순차 정규화로 구해야 하는데, 독립 가정으로 단순 합산하면 강한 말이 과대평가되는 방향으로 편향된다. 다음 실험은 그 유도식을 재는 것이어야 한다.

### 5-6. top-1 (참고, 온도 무관 — 단일 모델)

| 모델 | top-1 | vs LGB (paired 95% CI) |
|---|---:|---|
| LightGBM 73 | 33.49% | 기준 |
| S3 L20 단일 | 34.37% | +0.88 [−1.28, +3.11] |
| S3 L20 seed5 평균 | 35.09% | +1.59 [−0.32, +3.59] |

이 커밋에서 넘어야 할 선은 33.49 + 2×0.85 = **35.19**. seed5 평균 35.09 가 0.1%p 미달이고 CI 가 0 을 품는다 — 판정은 여전히 "차이 없음".
