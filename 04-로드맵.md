# 04 · 로드맵

## 주차별

| 주차 | 할 것 | 나오는 것 |
|---|---|---|
| **1–2** | **데이터 적재 + 피처 스토어** | **여기 막히면 전부 정지** |
| 2 | 조건부 로지스틱 베이스라인 | 비교 기준선 확보 |
| 3 | 경력 시퀀스 인코더 | "딥러닝 썼다"의 본체 |
| 3–4 | 경주 Attention + 6축 타워 | 차별점 완성 |
| 4 | 확률 보정 (+ 필요시 몬테카를로) | 서비스용 확률 |
| 4–5 | LangGraph 경기 후 해설 | LLM 파트 입문 |
| 5 | LangGraph 대화형 분석가 튜닝 | 자연어 슬라이더 완성형 |
| 5–6 | 백테스트 + 프리셋 3종 실측 | 온보딩 숫자 확정 |

> **순서를 지켜야 하는 이유**
> 베이스라인(2주차)을 시퀀스 인코더(3주차)보다 먼저 하지 않으면,
> 성능이 안 나올 때 **손실함수 문제인지 구조 문제인지 구분이 안 된다.**

---

## AI 파트 전체 범위

| # | 모듈 | 하는 일 | 기술 | 크기 |
|---|---|---|---|---|
| 1 | 피처 스토어 | 원본 → 30개 피처, 미래정보 차단 | 데이터 엔지니어링 | 큼 |
| 2 | 베이스라인 | 조건부 로지스틱 회귀 | 통계 | 작음 |
| 3 | **경력 인코더** | 말의 출전 이력을 시퀀스로 학습 | Transformer | **큼** |
| 4 | 경주 상호작용 | 같은 경주 말끼리 서로 참조 | Self-Attention | 중 |
| 5 | **6축 타워** | 한 모델 6갈래 + 랜덤 가중치 학습 | 멀티태스크 | 중 |
| 6 | 확률 보정 | "41%"를 진짜 41%로 | 캘리브레이션 | 중 |
| 7 | 시뮬레이션 | 조합 확률 (조건부 — 단승만이면 생략) | 몬테카를로 | 중 |
| 8 | 백테스트 | 시간순 검증, ROI, 프리셋 실측 | 평가 하네스 | 중 |
| 9 | 대화형 튜닝 | 도구 3개 + 대화 루프 | LangGraph | 중 |
| 10 | 경기 후 해설 | 축 기여도(`wₖ·sₖ`)를 문장으로 | LangGraph | 중 |
| 11 | 용어 Q&A | 정보 탭 | RAG | 중 |

**3·4·5번이 "직접 만드는 AI"의 본체.** PyTorch로 바닥부터 짜서 우리 데이터로 학습시킨다.
9·10·11번은 남의 모델(LLM)을 빌려 쓰는 것 — 조연.

---

## 백엔드로 붙는 볼륨

- **축 스코어 캐시 그리드** — `sₖ`를 미리 계산해 저장. 학습 데이터 범위(기간 × 경마장 ≈ 9종)까지 조합하면 캐시 설계가 커진다
- **실시간 결합 API** — 슬라이더 조작 → 60ms 안에 확률 반환
- **채점 배치** — 경기 발주 → 결과 수집 → 예측 채점 → 랭킹 갱신
- **배당 스냅샷 수집기** — 발주 N분 전 값 확보
- **10명 매칭 + 중간 베팅** — 실시간 상태 관리, `RacePool.sol` 연동

> "모델을 안 바꾸고 개인화를 서빙한다"는 게 백엔드 쪽에도 좋은 이야깃거리.
> 유저 수만큼 모델을 두는 대신 스코어를 캐시해 결합만 한다.

### 성능 시나리오 (컨설턴트 요청사항 — 문서 한 장 만들 것)

```
축 점수 s₁~s₆  ← 미리 계산해서 캐시 (경기당 1회)
       ↓
유저 슬라이더  → w₁·s₁ + … + w₆·s₆  → 60ms
```

곱셈 여섯 번. 유저가 100명이든 1만 명이든 모델을 다시 안 돌린다.
실제로 무거운 건 몬테카를로 하나뿐이고, 그것도 미리 돌려 캐시하면 된다.
→ 예상 DAU / 스파이크 트래픽 / 인프라 비용 추정치를 표로 만들어 다음 미팅에 가져갈 것.

---

## 인원 배분 — "각자 모델 하나씩"은 권하지 않음

6축은 독립 모델 6개가 아니라 **같은 파이프라인 × 피처 서브셋 6개**다.
코드의 90%가 공통(데이터 로딩, as-of 계산, 시간분할 CV, 캘리브레이션, 평가).

각자 만들면:
- 서로 다른 6벌의 전처리가 나와 통합할 때 재작성
- **스코어 척도가 안 맞아 가중합이 무의미해짐** (제일 치명적)
- 한 명만 as-of를 실수해도 그 축이 오염됨

그리고 레포에 `front/`, `back/chain/`, `infra/`가 다 있다.
**6명 전원이 모델을 잡으면 가장 비싼 일(데이터 수집·정합)을 아무도 안 맡는 배분이 된다.**

### 권장 배분

| 역할 | 내용 |
|---|---|
| ① 데이터 (1~2명) | KRA API 적재, as-of 피처 스토어 — **크리티컬 패스** |
| ② 학습·평가 (1명) | 단계별 모델, 시간분할 백테스트, 캘리브레이션 |
| ③ 서빙·백엔드 (1명) | 캐시 그리드, 결합 API, 채점 배치 |
| ④ LangGraph·LLM (1명) | 해설·대화형 튜닝·RAG |
| ⑤~⑥ 프론트/체인/인프라 | 온보딩, 슬라이더, 3D 재생, `RacePool.sol` |

### 절충안 — 축 해커톤

①②가 코어를 끝내고 **스코어 계약이 고정된 뒤**, 하루짜리로 각자 자기 축의
피처 엔지니어링만 담당. "내 모델을 만들어봤다"는 경험은 남고 통합 리스크는 0.

---

## 포트폴리오

### 등급

| 등급 | 항목 | 이유 |
|---|---|---|
| **A** | 랭킹 학습 (분류가 아니라 순위) | 검색·추천 회사의 핵심 기술. 주니어 중에 드묾 |
| **A** | 재학습 없는 성향 조절 | **아무도 안 해봄.** 설명하면 면접관이 되묻는다 |
| **A** | 시간 누수 차단 (as-of) | 실무에서 제일 중요한데 다들 모른다 |
| **B+** | 캘리브레이션 + ROI 백테스트 | "정확도만 보지 않는다"는 성숙도 신호 |
| **B** | 시퀀스 인코더 | 딥러닝 쓰는 정당한 이유의 교과서 사례 |
| C | 몬테카를로 | 나쁘진 않은데 흔함 |
| **D** | RAG / LangGraph | **부트캠프마다 다 있음.** 배관이지 모델이 아님 |
| **D** | LLM 요약 생성 | 프롬프트 작성. 기술적 신호 거의 없음 |

D는 서비스엔 필요하지만 **포트폴리오 앞면에는 안 세운다.**
"이 사람도 RAG 챗봇 만들었구나"로 묻힌다.

### 한 문장 피치

> **"경마 예측을 분류가 아니라 랭킹 문제로 풀었고, 사용자가 6개 관점의 비중을
> 실시간으로 바꿔도 재학습 없이 동작하는 구조를 만들었습니다."**

### 예상 질문 3개

**Q. 왜 분류가 아니라 랭킹이죠?**
경마는 "이 말이 1등 할까 O/X"가 아니라 "이 10마리 중 누가 앞서나"의 문제입니다.
분류로 풀면 말을 한 마리씩 따로 보게 되는데, 실제로는 같은 경주에 누가 같이 뛰는지가
결과를 바꿉니다. 그래서 경주 하나를 통째로 입력받고 순위 확률을 직접 최적화했습니다.

**Q. 사용자가 가중치를 바꾸는데 왜 재학습이 안 필요하죠?**
모델을 6개 갈래로 나눠 각 갈래가 독립적인 점수를 내게 학습시켰습니다.
사용자 조작은 그 6개 점수의 결합 계수만 바꾸는 거라 곱셈·덧셈으로 끝납니다.
다만 그냥 만들면 학습 때 안 본 조합에서 무너져서, 학습 중에 결합 계수를 매번 랜덤으로 뽑아
어떤 조합이 와도 동작하게 했습니다.

**Q. 성능은 어떻게 검증했나요?**
정확도는 안 봤습니다. 캘리브레이션 — 41%라고 예측한 것들이 실제로 41% 맞았는지,
그리고 시간순 분할 백테스트에서의 수익률을 봤습니다. 정확도만 보면 인기마만 찍는 모델이
1등이 되는데, 수수료 때문에 실제로는 손해거든요.

### 도메인 프레이밍

경마·베팅 도메인이 회사에 따라 걸릴 수 있다. **도메인 대신 기술을 앞세운다.**

- ✕ "AI 경마 예측 서비스"
- ○ **"경주 단위 랭킹 예측 + 사용자 제어형 개인화 모델"**

후보 집합 안에서 순위를 매기고 사용자별로 가중치를 다르게 두는 문제는
**상품 추천·검색 결과 정렬과 수학적으로 같다.** 그 연결을 말할 수 있으면 도메인은 문제가 안 된다.

| 우리 | 검색·추천 |
|---|---|
| 경주 10마리 | 검색 결과 후보 문서들 |
| 말 순위 | 문서 랭킹 |
| 유저 6축 가중치 | 사용자별 개인화 |

### 기록 원칙

**나중에 정리하는 게 아니라 처음부터 남긴다.**
- 매주 내가 뭘 결정했고 왜 그렇게 했는지 한 줄씩 (`docs/이정원.md` 활용)
- **실패도 지우지 않는다** — "딥러닝을 썼는데 회귀보다 안 나왔고, 데이터가 1만 경주뿐이라
  표 형태에서는 단순 모델이 유리했다"가 성공만 있는 프로젝트보다 잘 읽힌다
- 팀 프로젝트 최대 함정: "저희 팀이 만들었습니다"는 가치가 0

---

## 리스크

| | 리스크 | 대응 |
|---|---|---|
| 🔴 | **데이터 적재 실패** | 1~2주차 전력. 막히면 전부 정지 |
| 🟡 | 딥러닝이 회귀를 못 이김 | 데이터 1만 경주면 충분히 가능. **비교 결과 자체가 발표 내용.** 시퀀스 인코더는 수동 요약보다 정보 손실이 적어 제일 안전한 베팅 |
| 🟡 | 인기도 축이 압도 | ROI 지표로 자동 해결 |
| 🟢 | 트래픽 | 캐시 구조로 구조적 해결 (곱셈 6번) |

---

## 남은 결정

- [ ] 삼복승·중간베팅을 파나? → 몬테카를로 유지 vs 삭제
- [ ] 경기 시작 인원 (4·5·6명 중)
- [ ] 학습/검증 분할 경계 날짜
- [ ] 배당 스냅샷 시점 (발주 몇 분 전?)
- [ ] 일본 AI 경마 서비스 조사 (컨설턴트 제안)
- [ ] 성능 시나리오 문서 작성
