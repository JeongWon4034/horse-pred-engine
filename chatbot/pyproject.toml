[project]
name = "kra-dictionary-chatbot"
version = "0.1.0"
description = "경마 AI 사전 챗봇 — 마사회 용어집 RAG"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "uvicorn>=0.32", "pydantic>=2.9"]

[tool.uv]
package = false
